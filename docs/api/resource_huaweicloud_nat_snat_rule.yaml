info:
  version: 
  title: resource_huaweicloud_nat_snat_rule
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: NAT
paths:
  /v2/{project_id}/nat_gateways/{gatewayId}/snat_rules/{ruleId}:
    delete:
      tag: NAT
      operationId: Delete
  /v2/{project_id}/snat_rules/{ruleId}:
    get:
      tag: NAT
      operationId: Get
    put:
      tag: NAT
      operationId: Update
  /v2/{project_id}/snat_rules:
    post:
      tag: NAT
      operationId: Create
  /v1/{project_id}/publicips/{id}:
    get:
      tag: EIP
      operationId: Get
