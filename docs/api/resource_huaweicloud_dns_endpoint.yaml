info:
    version: ""
    title: resource_huaweicloud_dns_endpoint
    description: ""
schemes:
    - https
host: huaweicloud.com
tags:
    - name: DNS
paths:
    /v1/{project_id}/subnets:
        GET:
            tag: VPC
    /v2.1/endpoints:
        POST:
            tag: DNS
    /v2.1/endpoints/{endpointID}:
        DELETE:
            tag: DNS
        GET:
            tag: DNS
        PUT:
            tag: DNS
    /v2.1/endpoints/{endpointID}/ipaddresses:
        GET:
            tag: DNS
        POST:
            tag: DNS
    /v2.1/endpoints/{endpointID}/ipaddresses/{ipaddress}:
        DELETE:
            tag: DNS
