info:
  version: 
  title: resource_huaweicloud_lb_loadbalancer
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: ELB
  - name: VPC
paths:
  /v2/{project_id}/elb/loadbalancers/{id}:
    delete:
      tag: ELB
      operationId: Delete
    get:
      tag: ELB
      operationId: Get
    put:
      tag: ELB
      operationId: Update
  /v2/{project_id}/elb/loadbalancers:
    post:
      tag: ELB
      operationId: Create
  /v2.0/{project_id}/loadbalancers/{id}/tags/action:
    post:
      tag: ELB
      operationId: Create
  /v2.0/{project_id}/loadbalancers/{id}/tags:
    get:
      tag: ELB
      operationId: Get
  /v1/ports/{id}:
    get:
      tag: VPC
      operationId: Get
    put:
      tag: VPC
      operationId: Update
