info:
    title: data_source_huaweicloud_identitycenter_groups
    description: Use this data source to get the Identity Center groups.
host: myhuaweicloud.com
tags:
    - name: IdentityCenter
servers:
    - url: https://identitycenter.myhuaweicloud.com
paths:
    /v1/identity-stores/{identity_store_id}/groups:
        GET:
            tag: IdentityCenter
            operationId: GetIdentityCenterGroups
            x-ref-api: GET /v1/identity-stores/{identity_store_id}/groups
