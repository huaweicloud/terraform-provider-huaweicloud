info:
  version: 
  title: resource_huaweicloud_vpc_address_group
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: VPC
paths:
  /v3/{project_id}/vpc/address-groups/{address_group_id}:
    delete:
      tag: VPC
      operationId: DeleteAddressGroup
    get:
      tag: VPC
      operationId: ShowAddressGroup
    put:
      tag: VPC
      operationId: UpdateAddressGroup
  /v3/{project_id}/vpc/address-groups:
    post:
      tag: VPC
      operationId: CreateAddressGroup
