info:
  version: 
  title: resource_huaweicloud_images_image_copy
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: IMS
paths:
  /v1/cloudimages/{imageId}/copy:
    post:
      tag: IMS
      operationId: WithinRegionCopy
  /v1/cloudimages/{imageId}/cross_region_copy:
    post:
      tag: IMS
      operationId: CrossRegionCopy
  /{project_id}/cloudimages/{imageID}:
    patch:
      tag: IMS
      operationId: Update
  /{project_id}/image/{id}/tags:
    get:
      tag: IMS
      operationId: Get
  /{project_id}/images/{id}/tags/action:
    post:
      tag: IMS
      operationId: Create
  /{project_id}/images/{imageID}:
    delete:
      tag: IMS
      operationId: Delete
    patch:
      tag: IMS
      operationId: Update
