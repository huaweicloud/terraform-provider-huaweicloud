info:
    title: resource_huaweicloud_organizations_trusted_service
    description: Manages an Organizations trusted service resource within HuaweiCloud.
host: myhuaweicloud.com
tags:
    - name: Organizations
servers:
    - url: https://organizations.cn-north-4.myhuaweicloud.com
paths:
    /v1/organizations/trusted-services:
        GET:
            tag: Organizations
            operationId: GetTrustedService
            x-ref-api: GET /v1/organizations/trusted-services
    /v1/organizations/trusted-services/disable:
        POST:
            tag: Organizations
            operationId: DeleteTrustedService
            x-ref-api: POST /v1/organizations/trusted-services/disable
    /v1/organizations/trusted-services/enable:
        POST:
            tag: Organizations
            operationId: CreateTrustedService
            x-ref-api: POST /v1/organizations/trusted-services/enable
