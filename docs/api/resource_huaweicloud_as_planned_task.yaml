info:
  version: 
  title: resource_huaweicloud_as_planned_task
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: AS
paths:
  /autoscaling-api/v1/{project_id}/scaling-groups/{groupID}/scheduled-tasks/{taskID}:
    delete:
      tag: AS
      operationId: Delete
    put:
      tag: AS
      operationId: Update
  /autoscaling-api/v1/{project_id}/scaling-groups/{groupID}/scheduled-tasks:
    get:
      tag: AS
      operationId: List
    post:
      tag: AS
      operationId: Create
