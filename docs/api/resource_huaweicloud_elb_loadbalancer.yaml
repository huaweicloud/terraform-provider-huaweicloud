info:
  version: 
  title: resource_huaweicloud_elb_loadbalancer
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: ELB
paths:
  /v3/{project_id}/elb/loadbalancers/{id}/availability-zone/{action}:
    post:
      tag: ELB
      operationId: AddAvailabilityZone
  /v3/{project_id}/elb/loadbalancers/{id}/force-elb:
    delete:
      tag: ELB
      operationId: ForceDelete
  /v3/{project_id}/elb/loadbalancers/{id}:
    delete:
      tag: ELB
      operationId: Delete
    get:
      tag: ELB
      operationId: Get
    put:
      tag: ELB
      operationId: Update
  /v3/{project_id}/elb/loadbalancers:
    post:
      tag: ELB
      operationId: Create
  /v2.0/{project_id}/loadbalancers/{id}/tags/action:
    post:
      tag: ELB
      operationId: Create
  /v2.0/{project_id}/loadbalancers/{id}/tags:
    get:
      tag: ELB
      operationId: Get
  /v1/{project_id}/publicips/{id}:
    delete:
      tag: EIP
      operationId: Delete
