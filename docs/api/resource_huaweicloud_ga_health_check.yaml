info:
    title: resource_huaweicloud_ga_health_check
    description: Manages a GA health check. resource within HuaweiCloud.
host: myhuaweicloud.com
tags:
    - name: GA
servers:
    - url: https://ga.myhuaweicloud.com
paths:
    /v1/health-checks:
        POST:
            tag: GA
            operationId: CreateHealthCheck
            x-ref-api: POST /v1/health-checks
    /v1/health-checks/{id}:
        DELETE:
            tag: GA
            operationId: DeleteHealthCheck
            x-ref-api: DELETE /v1/health-checks/{id}
        GET:
            tag: GA
            operationId: UpdateHealthCheckWaiting
            x-ref-api: GET /v1/health-checks/{id}
        PUT:
            tag: GA
            operationId: UpdateHealthCheck
            x-ref-api: PUT /v1/health-checks/{id}
