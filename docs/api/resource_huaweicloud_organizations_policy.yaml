info:
    title: resource_huaweicloud_organizations_policy
    description: Manages an Organizations policy resource within HuaweiCloud.
host: myhuaweicloud.com
tags:
    - name: Organizations
servers:
    - url: https://organizations.cn-north-4.myhuaweicloud.com
paths:
    /v1/organizations/policies:
        POST:
            tag: Organizations
            operationId: CreatePolicy
            x-ref-api: POST /v1/organizations/policies
    /v1/organizations/policies/{policy_id}:
        DELETE:
            tag: Organizations
            operationId: DeletePolicy
            x-ref-api: DELETE /v1/organizations/policies/{policy_id}
        GET:
            tag: Organizations
            operationId: GetPolicy
            x-ref-api: GET /v1/organizations/policies/{policy_id}
        PATCH:
            tag: Organizations
            operationId: UpdatePolicy
            x-ref-api: PATCH /v1/organizations/policies/{policy_id}
