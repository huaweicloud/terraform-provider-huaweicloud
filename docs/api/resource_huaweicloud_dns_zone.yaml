info:
    version: ""
    title: resource_huaweicloud_dns_zone
    description: ""
schemes:
    - https
host: huaweicloud.com
tags:
    - name: DNS
paths:
    /v2/{project_id}/DNS-private_zone/{resource_id}/tags:
        GET:
            tag: DNS
    /v2/{project_id}/DNS-private_zone/{resource_id}/tags/action:
        POST:
            tag: DNS
    /v2/{project_id}/DNS-public_zone/{resource_id}/tags:
        GET:
            tag: DNS
    /v2/{project_id}/DNS-public_zone/{resource_id}/tags/action:
        POST:
            tag: DNS
    /v2/zones:
        POST:
            tag: DNS
    /v2/zones/{zone_id}:
        DELETE:
            tag: DNS
        GET:
            tag: DNS
        PATCH:
            tag: DNS
    /v2/zones/{zone_id}/actions/set-proxy-pattern:
        POST:
            tag: DNS
    /v2/zones/{zone_id}/associaterouter:
        POST:
            tag: DNS
    /v2/zones/{zone_id}/disable-dnssec:
        POST:
            tag: DNS
    /v2/zones/{zone_id}/disassociaterouter:
        POST:
            tag: DNS
    /v2/zones/{zone_id}/dnssec:
        GET:
            tag: DNS
    /v2/zones/{zone_id}/enable-dnssec:
        POST:
            tag: DNS
    /v2/zones/{zone_id}/statuses:
        PUT:
            tag: DNS
