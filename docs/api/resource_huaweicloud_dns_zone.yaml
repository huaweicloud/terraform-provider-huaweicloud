info:
    version: ""
    title: resource_huaweicloud_dns_zone
    description: ""
schemes:
    - https
host: huaweicloud.com
tags:
    - name: DNS
paths:
    /v2/{project_id}/{resourceType}/{id}/tags:
        GET:
            tag: DNS
    /v2/{project_id}/{resourceType}/{id}/tags/action:
        POST:
            tag: DNS
    /v2/zones:
        POST:
            tag: DNS
    /v2/zones/{zoneID}:
        DELETE:
            tag: DNS
        GET:
            tag: DNS
        PATCH:
            tag: DNS
    /v2/zones/{zoneID}/associaterouter:
        POST:
            tag: DNS
    /v2/zones/{zoneID}/disassociaterouter:
        POST:
            tag: DNS
