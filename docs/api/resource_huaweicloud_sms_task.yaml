info:
  version: 
  title: resource_huaweicloud_sms_task
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: SMS
paths:
  /v1/{project_id}/cloudservers/{serverID}:
    get:
      tag: ECS
      operationId: Get
  /v3/sources/{id}:
    get:
      tag: SMS
      operationId: Get
  /v3/tasks/{id}/action:
    post:
      tag: SMS
      operationId: Action
  /v3/tasks/{id}:
    delete:
      tag: SMS
      operationId: Delete
    get:
      tag: SMS
      operationId: Get
  /v3/tasks:
    post:
      tag: SMS
      operationId: Create
