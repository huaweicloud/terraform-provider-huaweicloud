info:
  version: 
  title: resource_huaweicloud_dns_resolver_rule
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: DNS
paths:
  /v2.1/resolverrules/{resolverRuleID}:
    delete:
      tag: DNS
      operationId: Delete
    get:
      tag: DNS
      operationId: Get
    put:
      tag: DNS
      operationId: Update
  /v2.1/resolverrules:
    post:
      tag: DNS
      operationId: Create
