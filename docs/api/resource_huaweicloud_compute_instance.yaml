info:
  version: 
  title: resource_huaweicloud_compute_instance
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: ECS
paths:
  /v2/cloudimages:
    get:
      tag: IMS
      operationId: List
  /v1/{project_id}/cloudservers/action:
    post:
      tag: ECS
      operationId: PowerAction
  /v1/{project_id}/cloudservers/delete:
    post:
      tag: ECS
      operationId: Delete
  /v1/{project_id}/cloudservers/{id}/os-reset-password:
    put:
      tag: ECS
      operationId: ChangeAdminPassword
  /v1/{project_id}/cloudservers/{id}/tags/action:
    POST:
      tag: ECS
      operationId: batchUpdate
  /v1/{project_id}/cloudservers/{server_id}/block_device/{volume_id}:
    get:
      tag: ECS
      operationId: Get
  /v1/{project_id}/cloudservers/{serverID}/metadata/{key}:
    delete:
      tag: ECS
      operationId: DeleteMetadatItem
  /v1/{project_id}/cloudservers/{serverID}/metadata:
    post:
      tag: ECS
      operationId: UpdateMetadata
  /v1.1/{project_id}/cloudservers/{serverID}/resize:
    post:
      tag: ECS
      operationId: Resize
  /v1/{project_id}/cloudservers/{serverID}:
    get:
      tag: ECS
      operationId: Get
    put:
      tag: ECS
      operationId: Update
  /v1.1/{project_id}/cloudservers:
    post:
      tag: ECS
      operationId: CreatePrePaid
  /v2.1/{project_id}/cloudvolumes/{id}/action:
    post:
      tag: EVS
      operationId: ExtendSize
  /v2/{project_id}/cloudvolumes/{id}:
    get:
      tag: EVS
      operationId: Get
  /v2.0/ports/{id}:
    put:
      tag: VPC
      operationId: Update
  /v2.1/{project_id}/servers/{id}/action:
    post:
      tag: ECS
      operationId: RemoveServer
  /v1/{project_id}/security-groups:
    get:
      tag: VPC
      operationId: List
  /v1/{project_id}/subnets/{id}:
    get:
      tag: VPC
      operationId: Get
