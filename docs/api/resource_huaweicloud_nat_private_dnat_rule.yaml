info:
  version: 
  title: resource_huaweicloud_nat_private_dnat_rule
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: NAT
paths:
  /v3/{project_id}/private-nat/dnat-rules/{ruleId}:
    delete:
      tag: NAT
      operationId: Delete
    get:
      tag: NAT
      operationId: Get
    put:
      tag: NAT
      operationId: Update
  /v3/{project_id}/private-nat/dnat-rules:
    post:
      tag: NAT
      operationId: Create
