info:
    title: data_source_huaweicloud_cnad_advanced_instances
    description: Use this data source to get the list of CNAD advanced instance.
host: myhuaweicloud.com
tags:
    - name: CNAD
servers:
    - url: https://aad.myhuaweicloud.com
paths:
    /v1/cnad/packages:
        GET:
            tag: CNAD
            operationId: GetAdvancedInstances
            x-ref-api: GET /v1/cnad/packages
