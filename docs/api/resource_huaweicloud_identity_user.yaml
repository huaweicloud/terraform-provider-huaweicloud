info:
  version: 
  title: resource_huaweicloud_identity_user
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: IAM
paths:
  /v3.0/OS-USER/users/{userID}:
    get:
      tag: IAM
      operationId: Get
    put:
      tag: IAM
      operationId: Update
  /v3.0/OS-USER/users:
    post:
      tag: IAM
      operationId: Create
  /v3/users/{userID}:
    delete:
      tag: IAM
      operationId: Delete
    get:
      tag: IAM
      operationId: Get
    patch:
      tag: IAM
      operationId: Update
  /v3.0/users:
    post:
      tag: IAM
      operationId: Create
