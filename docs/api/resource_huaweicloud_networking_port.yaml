info:
  version: refs/heads/master
  title: resource_huaweicloud_networking_port
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: VPC
paths:
  /v2.0/ports/{id}:
    delete:
      tag: VPC
      operationId: Delete
    get:
      tag: VPC
      operationId: Get
    put:
      tag: VPC
      operationId: Update
  /v2.0/ports:
    post:
      tag: VPC
      operationId: Create
